<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.dct_journal">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:name=".ScanDCTApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/dct_journal_mobile_with_wifi_network"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/dct_journal_mobile_with_wifi_network"
        android:supportsRtl="true"
        tools:targetApi="31">

        <activity
            android:name=".presentation.view.MainActivity"
            android:exported="false" />

        <activity
            android:name=".presentation.view.DeregistrationActivity"
            android:exported="false" />
    </application>

</manifest>